/**
 * 根级别：getters
 * 类似于：computed计算属性
 */

/**
 *
 *
 * @param {*} state
 * @returns
 */
// export const getDoneTrueList = state => {
//   //使用es6语法中的数组中的filter方法，过滤done为true的数据
//   let newArr = [];
//   newArr = state.todos.filter(todo => {
//     return !todo.done;
//   });
//   console.log(newArr);
//   return newArr;
// };

// //获取:当前歌曲播放下标
// export const currentIndex = state => state.currentIndex

// //获取:当前选中歌曲
// export const currentSong = (state) => {
//   return state.playlist[state.currentIndex] || {}
// }
